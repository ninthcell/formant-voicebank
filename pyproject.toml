[project]
name = "formant-voicebank"
version = "1.0.0"
description = "Formant-synthesized UTAU voice bank generator"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
